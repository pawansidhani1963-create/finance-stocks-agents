---
description: A description of your rule
---

The repo contains code for financial analysis of stocks
- Stock related data in jsons can be seen in `data` and subfolders inside it
- the real code starts under `src`
- `src\agents` has the code for agents and its tools
- `src\retrievers` has the code to extract different types of data from SEC filings